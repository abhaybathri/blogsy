function Logo({ width = '100px' }) {
    return (
        <div className="font-bold text-2xl tracking-tight" style={{ width, color: 'var(--text-primary)' }}>
            Blogsy
        </div>
    )
}
export default Logo
